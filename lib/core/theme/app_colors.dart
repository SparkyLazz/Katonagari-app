import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Backgrounds
  static const Color bg           = Color(0xFF0D0D0D);
  static const Color surface      = Color(0xFF141414);
  static const Color surfaceEl    = Color(0xFF1A1A1A);

  // Borders
  static const Color border       = Color(0xFF1E1E1E);
  static const Color borderStrong = Color(0xFF2A2A2A);

  // Accent (gold)
  static const Color accent       = Color(0xFFC4A778);
  static const Color accentDim    = Color(0x1AC4A778); // 10% opacity
  static const Color accentMuted  = Color(0x40C4A778); // 25% opacity

  // Text
  static const Color textPrimary   = Color(0xFFF0EBE0);
  static const Color textSecondary = Color(0xFFD0C8B8);
  static const Color textMuted     = Color(0xFF7A7060);
  static const Color textDim       = Color(0xFF3E3830);
  static const Color textGhost     = Color(0xFF2E2A26);

  // Semantic
  static const Color incomeGreen    = Color(0xFF5A9E6F);
  static const Color incomeGreenDim = Color(0x1F5A9E6F); // 12% opacity
  static const Color expenseRed     = Color(0xFFC45A5A);
  static const Color expenseRedDim  = Color(0x1FC45A5A); // 12% opacity
}